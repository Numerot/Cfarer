extends Area2D
#var entryCheck = %WanderingEntrycheck
#
#func _physics_process(delta: float) -> void:
	#if %Player.boatMode == false and entryCheck.playerHere == true:
		#for body in get_overlapping_bodies():
			#if body.name = "Player":
				#
				
